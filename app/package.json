{
  "name": "nodejs-api",
  "version": "1.0.0",
  "description": "Example Node.js API with Cognito authentication",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
